Lista — itens verticais {title, description, meta}; renderItem para custom.

```jsx
<ListView items={[{ id: 1, title: 'Entrevista técnica', description: 'Com Ana Souza', meta: 'há 2 dias' }]} onItemClick={abrir} />
```
