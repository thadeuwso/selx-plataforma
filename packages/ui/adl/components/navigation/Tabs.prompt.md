Abas — visões do mesmo contexto; sublinhado na ativa; contadores opcionais.

```jsx
<Tabs active={tab} onChange={setTab} tabs={[
  { id: 'perfil', label: 'Perfil' },
  { id: 'candidaturas', label: 'Candidaturas', count: 3 },
]} />
```
