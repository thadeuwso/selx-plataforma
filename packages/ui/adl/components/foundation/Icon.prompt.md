Ícone Lucide — a família única de ícones do ADL; usar para todo glifo de interface.

```jsx
<Icon name="search" size={16} />
<Icon name="trash-2" size={16} label="Excluir" />
```

Nomes em kebab-case (lucide.dev). Sem label = decorativo (aria-hidden). Cor herda currentColor; tamanhos 16/20/24.
