Combobox com busca — escolha única em lista longa, filtrada por digitação, navegável por teclado.

```jsx
<Combobox label="Cargo" value={cargo} onChange={setCargo}
  options={cargos} placeholder="Buscar cargo…" />
```
