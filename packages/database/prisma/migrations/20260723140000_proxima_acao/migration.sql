-- CreateTable
CREATE TABLE "TGPPROX" (
    "CODPROX" BIGSERIAL NOT NULL,
    "CODTEN" BIGINT NOT NULL,
    "CODFUN" BIGINT NOT NULL,
    "ACAO" TEXT NOT NULL,
    "PRIORIDADE" TEXT NOT NULL DEFAULT 'MEDIA',
    "PRAZO" DATE,
    "CODUSURESP" BIGINT,
    "ORIGEM" TEXT NOT NULL DEFAULT 'MANUAL',
    "JUSTIFICATIVA" TEXT,
    "STATUS" TEXT NOT NULL DEFAULT 'ABERTA',
    "DHCONCLUSAO" TIMESTAMP(3),
    "DHINC" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "CODUSUINC" BIGINT,
    "DHALT" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "TGPPROX_pkey" PRIMARY KEY ("CODPROX")
);

-- CreateIndex
CREATE INDEX "TGPPROX_CODTEN_CODFUN_STATUS_idx" ON "TGPPROX"("CODTEN", "CODFUN", "STATUS");

-- AddForeignKey
ALTER TABLE "TGPPROX" ADD CONSTRAINT "TGPPROX_CODTEN_fkey" FOREIGN KEY ("CODTEN") REFERENCES "TSXTEN"("CODTEN") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "TGPPROX" ADD CONSTRAINT "TGPPROX_CODFUN_fkey" FOREIGN KEY ("CODFUN") REFERENCES "TFPFUN"("CODFUN") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "TGPPROX" ADD CONSTRAINT "TGPPROX_CODUSURESP_fkey" FOREIGN KEY ("CODUSURESP") REFERENCES "TSIUSU"("CODUSU") ON DELETE SET NULL ON UPDATE CASCADE;



-- SelX: RLS (ADR-0002) — próximas ações são dados do funcionário, por tenant.
DO $$
DECLARE t text;
BEGIN
  FOREACH t IN ARRAY ARRAY['TGPPROX'] LOOP
    EXECUTE format('ALTER TABLE %I ENABLE ROW LEVEL SECURITY', t);
    EXECUTE format('ALTER TABLE %I FORCE ROW LEVEL SECURITY', t);
    EXECUTE format('CREATE POLICY politica_tenant ON %I USING ("CODTEN" = NULLIF(current_setting(''app.codten'', true), '''')::bigint) WITH CHECK ("CODTEN" = NULLIF(current_setting(''app.codten'', true), '''')::bigint)', t);
  END LOOP;
END $$;
